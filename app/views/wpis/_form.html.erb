<%= form_for(@wpi) do |f| %>
  <% if @wpi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wpi.errors.count, "error") %> prohibited this wpi from being saved:</h2>

      <ul>
      <% @wpi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tytul %><br />
    <%= f.text_field :tytul %>
  </div>
  <div class="field">
    <%= f.label :tresc %><br />
    <%= f.text_area :tresc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
