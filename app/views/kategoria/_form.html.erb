<%= form_for(@kategorium) do |f| %>
  <% if @kategorium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kategorium.errors.count, "error") %> prohibited this kategorium from being saved:</h2>

      <ul>
      <% @kategorium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nazwa %><br />
    <%= f.text_field :nazwa %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
