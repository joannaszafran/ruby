<h1>Listing uzytkowniks</h1>

<table>
  <tr>
    <th>Login</th>
    <th>Haslo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @uzytkowniks.each do |uzytkownik| %>
  <tr>
    <td><%= uzytkownik.login %></td>
    <td><%= uzytkownik.haslo %></td>
    <td><%= link_to 'Show', uzytkownik %></td>
    <td><%= link_to 'Edit', edit_uzytkownik_path(uzytkownik) %></td>
    <td><%= link_to 'Destroy', uzytkownik, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Uzytkownik', new_uzytkownik_path %>
